<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="SplashActivityTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.ModelTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 기본 버튼 스타일 지정 -->
        <item name="android:buttonStyle">@style/GreenButtonTheme</item>

    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- 기본 버튼 스타일 지정 -->
        <item name="android:buttonStyle">@style/GreenButtonTheme</item>
    </style>

    <style name="Theme.ModelTest" parent="Base.Theme.ModelTest" />
</resources>